

spring:
  application:
    name: PlaylistApp

  datasource:
    url: jdbc:postgresql://localhost:5432/playlistdb
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

server:
  port: 8080



